package test.unit;

import static org.junit.Assert.assertArrayEquals;
import static org.junit.Assert.assertEquals;

import org.junit.Test;

import main.Candidate;
import main.IR_Ballot;
import main.IR_Election;;

public class IR_Election_Tests {
    // @Test
    // // public void testIR_Election() {
    // //     IR_Eleciton ir = new IR_Election(electionFile, date);
    // //     assertEquals(1, a);

    // //     private Candidate [] candidates;
    // //     private int numCandidates;
    // //     private int numRemainingCandidates;
    // //     private IR_Ballot [] currentBallots;
    // //     private int currentBallotCount;
    // //     private IR_Audit audit; 

    // //     public IR_Election(Scanner electionFile, String date) {
    // //         super(electionFile);
    // //         this.typeElection = "IR Election";
    // //         audit = new IR_Audit(date);
    // //     }
    // }
}
